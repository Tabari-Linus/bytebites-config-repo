server:
  port: 9003 

management:
  endpoints:
    web:
      exposure:
        include: health, info, beans

eureka:
  instance:
    prefer-ip-address: true
    ip-address: auth-service
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/